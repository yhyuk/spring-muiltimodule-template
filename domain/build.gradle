plugins {
    id 'org.springframework.boot'
    id 'java-library'
}

dependencies {
    // JPA
    api 'org.springframework.boot:spring-boot-starter-data-jpa'
}

bootJar {
    enabled = false
}

jar {
    enabled = true
}
